plugins {
    id 'java'
}

group 'com.zyramc.ojvzin'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()

    //Repositório Spigot
    maven {
        url 'https://repo.codemc.org/repository/nms/'
    }
}

dependencies {
    compileOnly 'org.spigotmc:spigot:1.8.8-R0.1-SNAPSHOT'
    implementation files('C:/Users/joaov/Desktop/kCore.jar')
    implementation files('C:/Users/joaov/Desktop/kCosmetics.jar')
}

test {
    useJUnitPlatform()
}

compileJava.options.encoding = 'UTF-8'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}